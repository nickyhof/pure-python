These .pure files are vendored verbatim from the FINOS legend-pure project.

  repository: https://github.com/finos/legend-pure
  commit:     8f4632caebd6367ed8cb368a967a44f419df459e
  license:    Apache-2.0 (see the legend-pure repository)

Files and their source paths under legend-pure-core/legend-pure-m3-core/src/main/resources/platform/pure/:

  m3.pure        <- grammar/m3.pure
  relation.pure  <- relation.pure
  variant.pure   <- variant/variant.pure
  milestoning.pure <- grammar/milestoning.pure

m3.pure is the bootstrap of the Pure M3 metamodel: every metaclass (Class,
Property, Type, GenericType, Multiplicity, Enumeration, ...) is written as an
instance of meta::pure::metamodel::type::Class using Pure's low-level
instance-construction syntax. It is parsed by pure_python.codegen.parser.

relation.pure, variant.pure and milestoning.pure define further platform
types using Pure's ordinary readable class grammar (Class path<T> extends ...
{ prop : Type[mult]; }). They are parsed by pure_python.codegen.grammar.

The grammar/ subdirectory holds legend-pure's own ANTLR grammars, vendored from
the same commit, used to generate the parsers committed in
pure_python/codegen/_pure_antlr/. Source paths under legend-pure-core/:

  grammar/M3CoreLexer.g4   <- legend-pure-m3-core/.../m3parser/antlr/core/M3CoreLexer.g4
  grammar/M3CoreParser.g4  <- legend-pure-m3-core/.../m3parser/antlr/core/M3CoreParser.g4
  grammar/M4AntlrLexer.g4  <- legend-pure-m4/.../serialization/grammar/M4AntlrLexer.g4
  grammar/M4AntlrParser.g4 <- legend-pure-m4/.../serialization/grammar/M4AntlrParser.g4
  grammar/M4Fragment.g4    <- legend-pure-m4/.../serialization/grammar/core/M4Fragment.g4

The M3Core* grammars feed pure_python.codegen.grammar; the M4Antlr* grammars
feed pure_python.codegen.parser. M4Fragment.g4 is shared fragments imported by
both lexers (inlined by ANTLR at generation time, so it has no generated .py).
The only edit to any vendored grammar is one added line in M3CoreParser.g4 --
`options { tokenVocab=M3CoreLexer; }` -- so the split lexer/parser pair can be
generated standalone.

The pct/ subdirectory holds Legend's serialized PCT (Pure Compatibility Test)
data -- a pinned snapshot used by pure_python.report.pct to render the PCT
report (no JVM needed). These are the `pct-reports/*.json` resources extracted
verbatim from the published Legend jars (the same versions the legend-bridge
builds against: legend-pure 5.86.0, legend-engine 4.129.8):

  FUNCTIONS_<group>.json                  <- the function definitions (report rows)
  ADAPTER_<group>_compiled_Native.json    <- compiled-execution test results

  group = essential, grammar               from legend-pure-m3-core /
                                           legend-pure-runtime-java-engine-compiled
  group = standard, relation,              from legend-engine-pure-functions-<group>-pure /
          unclassified, variant            legend-engine-pure-runtime-java-extension-compiled-functions-<group>

The pure-python PCT column reflects the compiled-execution adapter -- the same
engine path LegendBridge.evaluate delegates to. To refresh, re-extract the
`pct-reports/*.json` resources from those jars at the desired versions.

To refresh: re-clone legend-pure at the desired commit, copy the files above,
then run `python -m pure_python.codegen.generate`.
